#!/bin/bash

# Example script to apply schema SQL files to PostgreSQL database on Azure

# Install PostgreSQL client if not already installed
# apt-get update
# apt-get install postgresql-client

# Apply schema SQL files to the database
# psql -h your-postgresql-host -U your-postgresql-user -d your-postgresql-db -a -f schema.sql
